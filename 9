#include <stdio.h>
#include <math.h>

int main (){
  float base, altura, area;

  scanf ("%f", &base);
  scanf ("%f", &altura);

  area: (base * altura)/2;  //fórmula da aréa do triângulo

  printf ("essa eh a area do triangulo: %.2f ");


return 0;
}

#include <stdio.h>
#include <stdlib.h>

int main (){
  int N,i, soma=0;

    printf ("Insira um numero:\n");
    scanf ("%d", &N);

    for (i=1; i<=N; i++){
        soma= soma + i;
    }
  printf ("%d", soma);
  
  return 0;
}
